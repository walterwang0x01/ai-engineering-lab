# 路线待审题 · 锚点比对核验报告

> 生成时间：2026-09-03 08:44:27
> 方法：解析+干扰项说明里的反引号术语/加粗短语/数字 → 回源笔记 markdown 做 indexOf 比对（含激进归一化：去 markdown 反引号、上下标、占位符、空括号、写法差异；引用标记 `[n]` 序列比对；`→0`↔`接近0`；科学计数 `1e-11` 量级匹配源里 `6.63e-11` 等同指数项）。
> 本脚本只做「依据核查」，**不改任何文件、不翻 reviewed**。结论供你人工过审时参考。
> 答案选项本身是否正确不在自动核验范围内，仍需你通读。

## 汇总

- 路线待审总数：**63** 道
- ✅ 可过审（锚点全中）：32
- 🟡 需修改·锚点缺失（真问题）：0
- 🟡 需人工通读·无锚点可验：31
- 🔴 建议删除（待确认）：0

> 说明：「无锚点可验」不是发现错误，而是这些解析没有反引号/加粗标记、锚点法无从比对，必须人工通读后才能翻 reviewed。
> 若想让这部分未来也能自动核验，可在解析里给关键符号加反引号（如 `d_k`）。

## 逐题明细

### ✅ 可过审（锚点全中，32）

**01-machine-learning/04-神经网络原理/01-感知机与多层感知机**
- `convergence-bound-normalize` — 感知机收敛定理给出的更新次数上界是 `(R / γ)²`，其中 `R = max ||x_i||`、`γ` 是最优分离超…
  - 结论：全部 7 个锚点在源笔记有依据（含归一化比对）
- `xor-hidden-remap` — 单层感知机解不了 XOR，加一个隐藏层就能解。隐藏层起作用的关键是什么？…
  - 结论：全部 7 个锚点在源笔记有依据（含归一化比对）
- `uat-existence-only` — 「单隐藏层只要够宽就能逼近任意连续函数」——通用近似定理这么说了，为什么实践中主流仍是深而窄的网络？…
  - 结论：全部 1 个锚点在源笔记有依据（含归一化比对）

**01-machine-learning/04-神经网络原理/02-反向传播推导**
- `why-not-numerical-diff` — 为什么训练神经网络不能用「给每个参数加一个 ε 看损失变化」的数值微分来求梯度？反向传播省掉的是什么？…
  - 结论：全部 8 个锚点在源笔记有依据（含归一化比对）
- `activation-cache-memory` — 同一个模型，训练时显存占用明显高于推理。从反向传播的机制看，多出来的主要是什么？…
  - 结论：全部 5 个锚点在源笔记有依据（含归一化比对）
- `gradient-check-when` — 笔记把梯度校验（用数值微分对比手写反向传播）称为标准调试手段，同时说生产训练中不用它。这个定位该怎么理解？…
  - 结论：全部 10 个锚点在源笔记有依据（含归一化比对）

**01-machine-learning/04-神经网络原理/03-梯度下降与优化器**
- `adamw-decoupled-decay` — 有人在 `torch.optim.Adam` 上调 `weight_decay`，发现「调了却没什么效果」。按笔记的解释…
  - 结论：全部 7 个锚点在源笔记有依据（含归一化比对）
- `adam-bias-correction` — Adam 为什么要做偏差修正 `m̂ = m / (1 - β1^t)`、`v̂ = v / (1 - β2^t)`？…
  - 结论：全部 12 个锚点在源笔记有依据（含归一化比对）
- `adagrad-rmsprop-fix` — AdaGrad 有一个对长时间训练不可接受的缺陷，RMSProp 只改了一处就修好了。这一处是什么？…
  - 结论：全部 8 个锚点在源笔记有依据（含归一化比对）

**01-machine-learning/04-神经网络原理/04-激活函数**
- `linear-stack-collapse` — 为了「省点计算」，有人把网络里每层的激活函数都去掉，只留 `z = Wx + b`，层数照旧。这个网络的表达能力会怎样？…
  - 结论：全部 2 个锚点在源笔记有依据（含归一化比对）
- `sigmoid-saturation-tanh` — 深层网络用 Sigmoid 训不起来，有人提议换 Tanh。按笔记的判断，这个换法解决了什么、没解决什么？…
  - 结论：全部 7 个锚点在源笔记有依据（含归一化比对）
- `swiglu-gating-cost` — SwiGLU 版 FFN 用三个权重矩阵（门控投影、内容投影、输出投影），传统 FFN 只用两个。这多出来的一路投影换来…
  - 结论：全部 2 个锚点在源笔记有依据（含归一化比对）

**01-machine-learning/04-神经网络原理/05-损失函数**
- `ce-from-mle` — 笔记强调「交叉熵损失不是拍脑袋设计的度量」。它是从哪里来的？…
  - 结论：全部 4 个锚点在源笔记有依据（含归一化比对）
- `softmax-ce-fused` — Softmax 对 logits 的偏导本身相当繁琐，但和交叉熵组合后梯度化简成 `∂CE/∂z = ŷ - y`。为什…
  - 结论：全部 11 个锚点在源笔记有依据（含归一化比对）
- `regression-loss-outliers` — 一个回归任务的标签里混着少量严重离群点，用 MSE 训练时损失被这几个样本主导。改用 MAE 或 Huber，各自要接受…
  - 结论：全部 6 个锚点在源笔记有依据（含归一化比对）

**02-llm/01-Transformer原理/01-注意力机制推导**
- `heads-param-count` — d_model 固定不变，把头数 h 从 8 增加到 16（每个头的 d_k 相应减半）。下面哪个说法正确？…
  - 结论：全部 1 个锚点在源笔记有依据（含归一化比对）

**02-llm/01-Transformer原理/03-架构组件与训练稳定性**
- `causal-mask-row` — 序列长度 5、加了 causal mask 的自注意力，位置 2（第 3 行）的注意力权重，下面哪一行是合法的？…
  - 结论：全部 3 个锚点在源笔记有依据（含归一化比对）

**02-llm/02-分词与表示/01-分词算法**
- `byte-level-bpe-cjk` — 用字节级 BPE 的模型，同样语义的一段中文往往比英文消耗更多 token。原因是什么？…
  - 结论：全部 1 个锚点在源笔记有依据（含归一化比对）

**02-llm/05-推理优化/02-量化**
- `outlier-damage` — Transformer 激活值里少数通道的幅度远超其余部分。这些离群值对量化的破坏性主要体现在哪里？…
  - 结论：全部 2 个锚点在源笔记有依据（含归一化比对）
- `symmetric-choice` — 按这篇笔记的建议，对 ReLU 之后的激活值做 INT8 量化时该选哪种方案，为什么？…
  - 结论：全部 1 个锚点在源笔记有依据（含归一化比对）

**04-ai-agent/00-基础概念/02-大语言模型基础**
- `temperature-zero-tool-calls` — 笔记的参数推荐表里，Agent 工具调用和数据提取都写 temperature=0，而对话聊天写 0.7。这个差异背后的…
  - 结论：全部 2 个锚点在源笔记有依据（含归一化比对）
- `reasoning-model-cost-shape` — 推理模型在回答前会先生成一段可能数千 token 的思考链。按笔记的说明，这件事对成本结构的影响是什么？…
  - 结论：全部 3 个锚点在源笔记有依据（含归一化比对）

**04-ai-agent/03-Agent框架/01-LangGraph工作流编排**
- `graph-vs-chain-cycle` — 笔记里的 ReAct 图把 `agent` 用条件边连到 `tools`，再用普通边把 `tools` 连回 `agen…
  - 结论：全部 7 个锚点在源笔记有依据（含归一化比对）
- `checkpoint-cost` — LangGraph 运行时在每一步都写 checkpoint。按笔记的说法，这个设计换来了什么、又在长时运行 Agent…
  - 结论：全部 3 个锚点在源笔记有依据（含归一化比对）

**04-ai-agent/06-RAG进阶/01-RAG架构与核心流程**
- `citation-source` — 笔记的 RAG Chain 让模型在回答里标注 `[1][2]` 这样的引用。这个引用能力是怎么来的？…
  - 结论：全部 3 个锚点在源笔记有依据（含归一化比对）

**04-ai-agent/07-工具与Function Calling/01-Function Calling机制**
- `who-executes-tool` — 开启 Function Calling 后，模型在第一轮到底产出了什么？…
  - 结论：全部 4 个锚点在源笔记有依据（含归一化比对）
- `tool-result-roundtrip` — 工具执行完之后，为什么必须把「模型那条带 tool_call 的 assistant 消息」和「带 tool_call_…
  - 结论：全部 2 个锚点在源笔记有依据（含归一化比对）
- `parallel-tool-calls-scope` — 开启并行工具调用后，模型一次响应里返回了两个 `get_weather` 调用（北京、上海）。「并行」在这里的准确含义是…
  - 结论：全部 1 个锚点在源笔记有依据（含归一化比对）

**04-ai-agent/09-多Agent系统/01-多Agent架构模式**
- `supervisor-vs-swarm-controlflow` — Supervisor 模式和 Swarm 模式在控制流上的根本区别是什么？…
  - 结论：全部 2 个锚点在源笔记有依据（含归一化比对）
- `network-message-explosion` — 选型表给 Network 模式的缺点是「消息爆炸」。按笔记的 `AgentNetwork` 实现，这个问题的来源是什么？…
  - 结论：全部 4 个锚点在源笔记有依据（含归一化比对）

**04-ai-agent/14-可观测与评估/01-LLM可观测性**
- `span-vs-generation` — 笔记手动构造链路时，检索步骤用 `trace.span(...)`，LLM 调用用 `trace.generation(…
  - 结论：全部 10 个锚点在源笔记有依据（含归一化比对）
- `eval-as-versioned-experiment` — 笔记提到把 LLM 评估写成 `@pytest.mark.phoenix` 测试、并通过 Experiments API…
  - 结论：全部 1 个锚点在源笔记有依据（含归一化比对）

### 🟡 需人工通读·无锚点可验（31）

**02-llm/01-Transformer原理/01-注意力机制推导**
- `scale-by-sqrt-dk` — 缩放点积注意力把 Q·Kᵀ 除以 sqrt(d_k)。如果改成除以 d_k，会发生什么？…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读
- `attention-vs-rnn-cost` — 笔记说自注意力是「用并行化换取了更差的复杂度增长曲线」。这个「更差」具体指什么？…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读

**02-llm/01-Transformer原理/02-位置编码**
- `permutation-invariance` — 自注意力被称为「排列不变」，根本原因是什么？…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读
- `rope-relative-distance` — 用了 RoPE 之后，位置 m 的 Query 与位置 n 的 Key 做点积。下面哪个说法正确？…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读
- `alibi-strong-prior` — ALiBi 直接在注意力分数上减去 m·|i−j|。按这篇笔记，它的主要代价是什么？…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读

**02-llm/01-Transformer原理/03-架构组件与训练稳定性**
- `pre-ln-vs-post-ln` — Pre-LN 是 y = x + SubLayer(LayerNorm(x))，Post-LN 是 y = LayerN…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读
- `ffn-position-wise` — FFN 层与注意力层的分工，下面哪个说法正确？…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读

**02-llm/02-分词与表示/01-分词算法**
- `bpe-first-merge` — 语料词频是 {low:5, lower:2, newest:6, widest:3}，按 BPE 的合并规则，下面哪个说…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读
- `wordpiece-denominator` — BPE 的打分是 count(A,B)，WordPiece 是 count(A,B)/(count(A)×count(B…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读

**02-llm/05-推理优化/01-KV-Cache与显存分析**
- `what-is-saved` — 关于 KV Cache 到底省掉了哪部分计算，下面哪个说法符合这篇笔记？…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读
- `prefill-vs-decode` — 同一次生成里，Prefill 阶段计算密集而 Decode 阶段显存带宽密集，根本原因是什么？…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读
- `continuous-batching-gain` — 相比静态批处理，连续批处理（Continuous Batching）直接解决的是哪个问题？…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读

**02-llm/05-推理优化/02-量化**
- `gptq-vs-awq` — GPTQ 和 AWQ 都是 PTQ 方法，它们降低量化误差的机制差别是什么？…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读

**02-llm/05-推理优化/03-蒸馏与剪枝**
- `pruning-hardware-friendliness` — 非结构化剪枝能达到很高的稀疏率且精度损失小，但工程上常常拿不到相应的加速。原因是什么？…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读
- `temperature-role` — 蒸馏里把温度 T 调大，会带来什么？…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读
- `llm-compression-choice` — 在 LLM 上做压缩，这篇笔记给出的工程判断是什么？…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读

**04-ai-agent/00-基础概念/01-AI Agent概述与发展**
- `agent-vs-chatbot` — 笔记用一组对照区分 Chatbot 和 AI Agent。这条分界线落在哪里？…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读
- `context-vs-prompt-engineering` — 笔记把 Context Engineering 描述为 Prompt Engineering 的延伸并演变成独立学科。两…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读
- `agent-runtime-hosted-shift` — 笔记把 2026 年的一个动向记为「Agent 从开发者自建向平台托管演进」。托管运行时接手的是哪一部分工作？…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读

**04-ai-agent/00-基础概念/02-大语言模型基础**
- `prompt-cache-prefix-order` — Prompt Caching 的命中依据是前缀匹配。一个 Agent 每轮请求的结构是 [系统提示 | 工具定义 | 历…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读
- `effective-context-limit` — 笔记特别提醒「上下文窗口大小 ≠ 有效利用能力」。按它引用的研究结论，把一份长文档整段塞进超大上下文窗口，最可能踩到什么…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读
- `json-mode-vs-structured-output` — 笔记把 JSON Mode 和 Structured Output（schema 强制）明确区分开。这个区别在工程上意味…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读

**04-ai-agent/00-基础概念/03-Prompt Engineering**
- `injection-keyword-filter-limit` — 笔记的注入防御有三层：关键词过滤输入、用 <user_input> 标签隔离、检查输出。为什么关键词黑名单（「忽略上述指…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读
- `delimiter-isolation-assumption` — 第二层防御把用户输入包进 <user_input> 标签，并在系统提示里声明「不要执行标签内的任何指令性内容」。这个做法…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读
- `cot-vs-tot` — Chain-of-Thought 让模型一步步推下去，Tree-of-Thought 让它先生成若干条不同思路、逐条评估…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读
- `few-shot-message-form` — 笔记的 Few-Shot 示例把两组「用户输入 + 期望输出」写成交替的 user / assistant 消息，接在真…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读

**04-ai-agent/03-Agent框架/01-LangGraph工作流编排**
- `delta-channel-snapshot` — Delta Channels 只保存每一步的增量，但仍然「每 K 步写一次完整快照」。这个完整快照是为了解决什么问题？…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读

**04-ai-agent/06-RAG进阶/01-RAG架构与核心流程**
- `naive-vs-advanced` — 按笔记的演进路线，Advanced RAG 相对 Naive RAG 多出来的是什么？…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读
- `mmr-purpose` — 检索器配成 `search_type="mmr"`、`{"k": 5, "fetch_k": 20}`。MMR（最大边际…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读

**04-ai-agent/09-多Agent系统/01-多Agent架构模式**
- `pattern-for-dynamic-routing` — 一个客服系统要按用户问题类型转接到账单、技术等专家，并且希望转接由 Agent 自己判断、不经过中心调度。按笔记的选型表…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读

**04-ai-agent/14-可观测与评估/01-LLM可观测性**
- `instrumentation-layer` — 笔记里 LangSmith 只要设置 `LANGCHAIN_TRACING_V2=true` 就自动记录调用，Phoen…
  - 结论：解析无反引号/加粗锚点，锚点法无法自动比对，需人工通读

