## 改了什么

<!-- 一句话说清这个 PR 解决什么问题，不要复述 diff -->

## 为什么这样改

<!-- 有取舍的地方说明理由。如果尝试过别的方案但不行，说明为什么 -->

## 验证

<!-- 贴出实际命令输出，不要只写「测试通过」 -->

```
pnpm run check:all
```

- [ ] `pnpm run lint` 0 问题
- [ ] `pnpm run check` 0 错误 0 警告
- [ ] `pnpm run test:unit` 全绿
- [ ] `pnpm run test:smoke` 全绿

## 涉及题目内容的 PR 额外确认

- [ ] 每道数值题都有测试**用独立公式重算**答案（不是复述题目里的数字）
- [ ] 每个选择题的干扰项都是真实的常见误解，且写了 `distractorNotes`
- [ ] 无法精确计算的数字（质量损失、加速比等）已标注「示意性估算」，或已删除
- [ ] 沙盒的可行解数量已用脚本枚举确认，且「一路选最省」会失败

## 涉及 UI 的 PR 额外确认

- [ ] 换题的地方用了 `{#key question.id}`
- [ ] 新增内部链接用了 `resolve()`
- [ ] 已实际看过渲染效果（截图或本地验证），不是只看代码
- [ ] 移动端宽度（390px）无横向溢出

## 检查过 AGENTS.md 吗

- [ ] 读过 [AGENTS.md](../AGENTS.md)，本 PR 不违反其中任何硬约定
