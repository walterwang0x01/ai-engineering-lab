[
	{
		"id": "cache-edits-skip",
		"prompt": "笔记里清理长会话的旧消息不是删除，而是把内容清空并标记 `cache_edit: 'skip'`、保留消息位置。为什么这样做能保住缓存？",
		"options": [
			"Prompt Cache 是按前缀字节匹配的，删掉中间的消息会改变后续所有内容的前缀；保留位置让缓存前缀维持原样，后面的消息仍然命中",
			"标记为 skip 的消息会被服务端单独缓存，所以删不删都不影响",
			"skip 让这些消息不计入输入 Token，删除则会计费，差别只在计价",
			"删除是异步生效的，skip 是同步生效的，所以 skip 更快"
		],
		"answerIndex": 0,
		"explanation": "笔记讲得很直接：传统做法删除 M2、M3 后缓存前缀从 [M1..M8] 变成 [M1]，后面 6 条全部失效；`cache_edits` 把它们标记为 skip、清空内容但保留位置，前缀仍是 [M1..M8]，全部命中。根因是缓存的匹配单位是「从头开始的字节前缀」——任何对靠前内容的改动都会让它之后的一切重新按未缓存价格处理。这也是「清掉数百条旧消息后下一轮还和第一轮一样快」的原因。",
		"hint": "缓存匹配的是「从头到某处的完整前缀」，不是单条消息。",
		"distractorNotes": {
			"1": "缓存不是按消息逐条存的，正是因为它按前缀整体匹配，中间动一下才会牵连后面全部。",
			"2": "计价只是结果。真正的因果是前缀被破坏后这些 Token 必须重新处理——于是既贵又慢，延迟从毫秒级跳到秒级。",
			"3": "同步异步与此无关；差别在于改动有没有破坏缓存前缀。"
		},
		"reviewed": false
	},
	{
		"id": "sticky-latch-tradeoff",
		"prompt": "笔记的 `StickyLatch` 在最短锁存时长内即使条件已经变化也继续返回旧值，用来防止频繁模式切换破坏缓存。这个机制付出的代价是什么，因此它的适用边界在哪？",
		"options": [
			"代价是系统提示会在一段时间内保持过时状态，所以只能用于「短暂陈旧不影响正确性」的段；必须及时生效的内容要标记为不参与缓存",
			"代价是内存占用随会话增长，所以只适合短会话",
			"代价是缓存写入费用翻倍，所以只在缓存读取足够多时才划算",
			"代价是丢失历史值无法回滚，所以需要额外持久化"
		],
		"answerIndex": 0,
		"explanation": "锁存的本质是「用一段时间的陈旧换缓存命中」。这对模式标识这类可以晚几分钟生效的内容是好买卖；但如果被锁住的是权限规则、当前用户之类必须立刻生效的内容，那就是正确性问题了。所以笔记同时给出配套设计：系统提示分为参与缓存的稳定段和被 `DANGEROUS_uncachedSystemPromptSection()` 标记的动态段，频繁变化的内容不进缓存前缀，也就不需要靠锁存去硬撑。",
		"hint": "问自己：被锁住的这段内容晚几分钟生效，会不会出事？",
		"distractorNotes": {
			"1": "锁存只保存一个当前值和一个时间戳，与会话长度无关。",
			"2": "锁存的目的正是减少缓存重写，它不会让写入费用翻倍。",
			"3": "旧值就是它主动保留的那个值，不存在丢失历史的问题，也不需要回滚语义。"
		},
		"reviewed": false
	},
	{
		"id": "autocompact-circuit-breaker",
		"prompt": "笔记记录的自动压缩故障是：压缩触发 → 失败 → 下一轮继续触发 → 继续失败，修复方式是给连续失败次数设上限。这个故障模式的关键特征是什么？",
		"options": [
			"触发条件在失败后依然成立，于是重试自身成了稳定的循环，失败次数不收敛——自动化流程必须有熔断，否则一次失败会被无限放大",
			"压缩逻辑有并发竞争，多个协程同时压缩同一份上下文",
			"压缩后的上下文比压缩前更长，因此立刻再次超过阈值",
			"失败被静默吞掉没有日志，所以无人发现"
		],
		"answerIndex": 0,
		"explanation": "触发压缩的条件是「上下文过长」。压缩失败后上下文还是那么长，条件依然满足，下一轮照样触发——失败没有改变导致失败的前提，于是形成自持循环，笔记里出现了单会话最多 3272 次连续失败。修复的三行代码就是给连续失败设上限（熔断），让重试在自我放大之前停下。笔记把这条归纳为「任何自动化流程都需要熔断机制」。",
		"hint": "失败之后，触发条件变了吗？",
		"distractorNotes": {
			"1": "笔记描述的是逐轮串行触发，不是并发竞争；根因是重试条件不收敛。",
			"2": "笔记没有说压缩产物更长。压缩根本没成功，上下文保持原样就已经足够让条件反复成立了。",
			"3": "失败是被记录下来了（正因如此才统计出 1279 个会话、每天约 25 万次浪费调用）。可见性不是根因，缺熔断才是。"
		},
		"reviewed": false
	}
]
